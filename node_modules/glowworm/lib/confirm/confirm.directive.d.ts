import { ElementRef, EventEmitter, OnDestroy, TemplateRef } from "@angular/core";
import { ComponentLoaderService } from "../core/component-loader.service";
export declare class GwConfirmDirective implements OnDestroy {
    private componentLoader;
    private el;
    title: string;
    content: string | TemplateRef<any> | any;
    confirmClass: string;
    zIndex: number;
    confirmText: string;
    cancelText: string;
    onConfirm: EventEmitter<void>;
    onCancel: EventEmitter<void>;
    private componentRef;
    constructor(componentLoader: ComponentLoaderService, el: ElementRef);
    onclick(): void;
    createComponent(): void;
    ngOnDestroy(): void;
}
