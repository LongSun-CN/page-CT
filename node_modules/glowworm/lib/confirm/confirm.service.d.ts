import { ComponentLoaderService } from "../core/component-loader.service";
import { GwConfirmConfig } from "./confirm.config";
export declare class GwConfirmService {
    private componentLoader;
    constructor(componentLoader: ComponentLoaderService);
    show(config: GwConfirmConfig): void;
    private _destroy(componentRef);
}
