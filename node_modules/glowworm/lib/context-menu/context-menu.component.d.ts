import { NgZone, OnDestroy, OnInit, Renderer2 } from "@angular/core";
import { ContextMenu } from "./context-menu";
import { ContextMenuService } from "./context-menu.service";
import { Subscription } from "rxjs/Subscription";
export declare class GwContextMenuContainerComponent implements OnInit, OnDestroy {
    private ngZone;
    private renderer;
    private service;
    class: any;
    styler: any;
    hidden: boolean;
    menus: ContextMenu[];
    menuSubscribe: Subscription;
    constructor(ngZone: NgZone, renderer: Renderer2, service: ContextMenuService);
    private _clickFun;
    private _menuFun;
    ngOnInit(): void;
    ngOnDestroy(): void;
    clickedOutside(event: MouseEvent): void;
    hide(): void;
    _onClickMenu(menu: ContextMenu): void;
    _showMenus(menus: ContextMenu[]): boolean;
    _showMenu(menu: ContextMenu): boolean;
    _disabledMenus(menu: ContextMenu): boolean;
    private _deepCloneMenus(menus);
}
